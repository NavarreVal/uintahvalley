# Old storefront paths → rebuild IA. Production cutover happens later.
# Do not add a /* splat here — it can intercept POST /api/contact on the
# custom domain (Pages still serves 404.html for unknown static paths).
/experimental.html    /shop.html      302
/experimental         /shop.html      302
/product.html         /shop.html      302
/product              /shop.html      302
